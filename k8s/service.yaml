apiVersion: v1
kind: Service
metadata:
  name: fraud-api-service
  namespace: fraud-detection
  labels:
    app: fraud-api
spec:
  type: LoadBalancer
  selector:
    app: fraud-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP